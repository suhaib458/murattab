# استيراد التقويم الجامعي مستقبلًا

ضع الملف الأصلي في مجلد `source/` ضمن مسار السنة/الفصل. تحوله أداة import مستقبلية، وتعرض الأحداث منخفضة الثقة فقط للمراجعة، ثم تعتمد `calendar.json` المتحقق منه عبر `pnpm calendar:validate`. بعد المراجعة يُنفذ commit ثم نشر Vercel. لا يوجد استخراج PDF أو AI في هذا الإصدار.
