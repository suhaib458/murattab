# الاختبارات

`pnpm test` يشغّل Vitest لمنطق الأيام والوقت والقاعات والجلسات والتعارض والتحقق والنسخ الاحتياطي وICS. `pnpm test:e2e` يشغّل Playwright لمسار onboarding والإضافة والحفظ والاستجابة؛ شغّل `pnpm exec playwright install chromium` مرة واحدة إذا لم يكن المتصفح مثبتًا في البيئة.
